/**
 * TODO Codex: implementar UI de login con Supabase Auth.
 * Opciones soportadas: email + password, OAuth con Google.
 * Tras login exitoso, redirigir a la ruta indicada en ?redirect= o a /chat por defecto.
 */

export default function LoginPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center px-6">
      <h1 className="text-3xl font-bold">Iniciar sesión</h1>
      <p className="mt-4 text-slate-600">Pendiente de implementar.</p>
    </main>
  );
}
